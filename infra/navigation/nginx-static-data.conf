    # Static OSM/GeoNames data served from /opt/mansoni/static-data
    location /data/osm/ {
        alias /opt/mansoni/static-data/osm/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
        try_files $uri =404;
    }
