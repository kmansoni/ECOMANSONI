# Secrets — never commit
.env

# Runtime data — generated on VPS
data/
certs/
certbot-www/
dms-setup.sh

# docker-mailserver generated config (contains passwords and DKIM private keys)
config/postfix-accounts.cf
config/opendkim/keys/
