# 🗺️ МАКСИМАЛЬНО ПОДРОБНЫЙ ROADMAP: Создание аналога Amap (高德地图)

**Проект:** mansoni navigation_system  
**Возраст:** 58 дней (18 февраля - 18 апреля 2026)  
**Цель:** 1000% аналог китайского навигатора Amap  
**Автор:** AI Engine  
**Обновлено:** 2026-04-18

---

## 📊 АНАЛИЗ ТЕКУЩЕГО СОСТОЯНИЯ

### Что уже есть (58 дней разработки)

| Компонент | Статус | Файлы |
|-----------|--------|-------|
| **Бэкенд** | ✅ Готов | `navigation_server/` |
| **API сервер** | ✅ FastAPI на 8000 порту | `main.py`, `config.py` |
| **Routing** | ✅ Valhalla интеграция | `routing_service.py` - маршрутизация, матрицы, TSP |
| **Geocoding** | ✅ Photon интеграция | `geocoding_service.py` |
| **POI** | ✅ PostGIS | `poi_service.py`, `models/poi.py` |
| **Traffic** | ✅ Kafka интеграция | `traffic_service.py` |
| **Location** | ✅ Redis real-time | `location_service.py` |
| **Dispatch** | ✅ Судя по названию | `dispatch_service.py` |
| **Crowdsource** | ✅ Сбор данных | `crowdsource_service.py` |
| **Risk/Surge** | ✅ Анализ рисков | `risk_service.py`, `surge_service.py` |
| **H3 Spatial** | ✅ Геоиндексирование | `h3_service.py` |

### Зазор до Amap (что НЕХВАТАЕТ)

| Функция | Текущий статус | Приоритет |
|---------|---------------|------------|
| Мобильное приложение (iOS/Android) | ❌ Нет | 🔴 Критический |
| UI карты (рендеринг) | ❌ Нет | 🔴 Критический |
| Turn-by-turn навигация | ❌ Нет (только API) | 🔴 Критический |
| Офлайн режим | ❌ Нет | 🔴 Критический |
| OpenGL рендеринг | ❌ Нет | 🔴 Критический |
| C++ движок | ❌ Нет | 🔴 Критический |
| Dynamic UI (JS->Native) | ❌ Нет | 🟠 Высокий |
| Bundle архитектура | ❌ Нет ( monolithic FastAPI) | 🟠 Высокий |
| Кросс-платформа | ❌ Нет | 🟡 Средний |
| AR навигация | ❌ Нет | 🟡 Средний |
| Голосовые подсказки (TTS) | ❌ Нет | 🟡 Средний |

---

# PHASE 0: ПОДГОТОВКА ИНФРАСТРУКТУРЫ (Дни 1-14)

## Неделя 1: Архитектура и планирование

### День 1-2: Анализ и проектирование

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.1: АРХИТЕКТУРНОЕ ПРОЕКТИРОВАНИЕ НАВИГАТОРА
─────────────────────────────────────────────────────────────────────────────

Задача: Спроектируй полную архитектуру навигатора аналогичного Amap.

Требования к архитектуре:

1. СЛОИ ПРИЛОЖЕНИЯ:
   - Presentation Layer: React/Vue web, iOS (Swift), Android (Kotlin)
   - Business Logic Layer: Python FastAPI (существующий), Node.js
   - Engine Layer: C++ (рендеринг карты, позиционирование)
   - Data Layer: PostgreSQL+PostGIS, Redis, tile storage

2. КОМПОНЕНТЫ ДВИЖКА (参考 Amap):
   - Map Renderer (C++/OpenGL ES)
   - Route Engine (существующий Valhalla - расширить)
   - Location Engine (GPS + сенсоры + Kalman filter)
   - Navigation Engine (turn-by-turn)
   - Data Engine (тайлы, POI, дороги)

3. КОММУНИКАЦИЯ:
   - gRPC между сервисами
   - WebSocket для real-time (существующий websocket в ai_engine)
   - REST для API (существующий FastAPI)

4. ТЕКУЩИЙ КОД ДЛЯ ИНТЕГРАЦИИ:
   - navigation_server/services/routing_service.py - расширить
   - navigation_server/services/geocoding_service.py - расширить
   - navigation_server/services/poi_service.py - расширить
   - navigation_server/services/traffic_service.py - расширить

Создай UML диаграмму компонентов в формате Mermaid.
Сохрани в файл docs/architecture/navigator-architecture.md
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.2: ПРОЕКТИРОВАНИЕ БАЗЫ ДАННЫХ ДЛЯ НАВИГАТОРА
─────────────────────────────────────────────────────────────────────────────

Задача: Спроектируй схему БД для навигатора.

Требования:

1. ТАБЛИЦЫ ДЛЯ НАВИГАЦИИ:
   - offline_regions: регионы для офлайн-карт
   - offline_tiles_meta: метаданные тайлов (z, x, y, region_id)
   - downloaded_maps: загруженные пользователем карты
   - favorite_locations: избранные места
   - navigation_history: история поездок
   - saved_routes: сохраненные маршруты
   - user_preferences: настройки навигации

2. РАСШИРЕНИЕ СУЩЕСТВУЮЩИХ:
   - Extend poi table с offline_available, last_updated
   - Extend location table с accuracy, altitude, heading

3. POSTGIS ИНТЕГРАЦИЯ:
   - Пространственные индексы для POI
   - H3 индексирование (используй существующий h3_service.py)
   - Маршрутизация через pgRouting

Создай миграции в supabase/migrations/
Документируй в docs/database/navigator-schema.md
```

### День 3-4: Настройка окружения разработки

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.3: НАСТРОЙКА CI/CD ДЛЯ НАВИГАТОРА
─────────────────────────────────────────────────────────────────────────────

Задача: Настрой CI/CD пайплайн для всех компонентов навигатора.

Требования:

1. GitHub Actions workflow для:
   - Python (FastAPI): тесты, линт, сборка Docker
   - TypeScript (web): тесты, линт, сборка
   - C++ (engine): компиляция, тесты
   - iOS: сборка (через macOS runner)
   - Android: сборка (через GitHub Actions)

2. ИСПОЛЬЗУЙ СУЩЕСТВУЮЩИЕ:
   - .github/workflows/ - существующие workflow
   - ai_engine/agent/ci_cd_system.py - как референс

3. ДЕПЛОЙ:
   - Docker compose для dev/staging
   - Kubernetes для production
   - GitHub Packages для артефактов

Создай .github/workflows/navigator-*.yml
Документируй в docs/ci-cd/setup.md
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.4: НАСТРОЙКА МОНИТОРИНГА И ЛОГИРОВАНИЯ
─────────────────────────────────────────────────────────────────────────────

Задача: Настрой полный мониторинг для навигатора.

Требования:

1. МЕТРИКИ (Prometheus + Grafana):
   - API latency (существующий в main.py есть prometheus)
   - Routing time (из routing_service.py)
   - Location update latency
   - Tile loading time
   - Memory/CPU usage
   - Error rates by type

2. TRACING (Jaeger или Zipkin):
   - Distributed tracing через все сервисы
   - Trace per request
   - Span для каждой операции

3. LOGGING (ELK или Loki):
   - Структурированные логи (structlog уже есть в main.py)
   - Логирование в JSON
   - Алерты на errors

4. HEALTH CHECKS:
   - /health эндпоинт для каждого сервиса
   - Readiness/liveness probes для k8s

Интегрируй с существующей системой (если есть)
Создай docs/monitoring/dashboard.json
```

### День 5-7: Базовые сервисы

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.5: РАСШИРЕНИЕ ROUTING SERVICE
─────────────────────────────────────────────────────────────────────────────

Задача: Расширь существующий routing_service.py для полной навигации.

Требования:

1. ДОБАВЬ НОВЫЕ МЕТОДЫ:
   - update_route_on_deviation() - пересчет при отклонении от маршрута
   - get_next_maneuver() - следующая команда навигации
   - calculate_eta() - точное время прибытия с учетом пробок
   - get_traffic_along_route() - пробки на маршруте

2. ИНТЕГРИРУЙ С:
   - traffic_service.py - получение пробок
   - location_service.py - текущее положение

3. ГОЛОСОВЫЕ ИНСТРУКЦИИ:
   - Генерируй текстовые инструкции для TTS
   - Поддержка нескольких языков
   - Разные голоса для типов маневров

Добавь методы в services/routing_service.py
Добавь тесты в tests/test_routing_extensions.py
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.6: СОЗДАНИЕ LOCATION SERVICE ДЛЯ REAL-TIME TRACKING
─────────────────────────────────────────────────────────────────────────────

Задача: Расширь location_service.py для real-time навигации.

Требования:

1. ФУНКЦИОНАЛ:
   - Регистрация устройства для трекинга
   - Real-time обновление позиции (WebSocket push)
   - History трек (пройденный путь)
   - Geofencing уведомления
   - Background location tracking (mobile)

2. ИНТЕГРИРУЙ С:
   - Redis для real-time (существующий)
   - Kafka для событий (существующий kafka_client.py)
   - H3 для пространственных запросов (h3_service.py)

3. АЛГОРИТМЫ:
   - Kalman filter для сглаживания GPS
   - Snap to road (привязка к дороге)
   - Dead reckoning для туннелей

Расширь services/location_service.py
Добавь модели в models/location.py
Добавь router в routers/location.py
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.7: СОЗДАНИЕ NAVIGATION SERVICE
─────────────────────────────────────────────────────────────────────────────

Задача: Создай новый сервис для turn-by-turn навигации.

Требования:

1. ОСНОВНЫЕ ФУНКЦИИ:
   - Start navigation (инициация)
   - Get current instruction (текущая команда)
   - Update position tracking
   - Recalculate on deviation
   - End navigation
   - Voice instruction queue

2. ИНТЕГРИРУЙ С:
   - routing_service - построение маршрута
   - location_service - позиция
   - traffic_service - пробки для пересчета

3. ДАННЫЕ:
   - Store active navigation sessions
   - Progress tracking
   - Arrival predictions

Создай services/navigation_service.py
Создай models/navigation.py
Создай routers/navigation.py
```

---

## Неделя 2: API и клиентская инфраструктура

### День 8-10: Мобильное API

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.8: REST API ДЛЯ МОБИЛЬНОГО КЛИЕНТА
─────────────────────────────────────────────────────────────────────────────

Задача: Создай REST API для мобильного приложения навигатора.

Требования:

1. ЭНДПОИНТЫ:
   POST /api/v1/navigation/start - начать навигацию
   GET  /api/v1/navigation/{session_id}/instruction - текущая команда
   POST /api/v1/navigation/{session_id}/position - обновить позицию
   GET  /api/v1/navigation/{session_id}/eta - ETA
   DELETE /api/v1/navigation/{session_id} - завершить

   GET  /api/v1/maps/offline - список доступных регионов
   POST /api/v1/maps/offline/{region_id} - скачать регион
   DELETE /api/v1/maps/offline/{region_id} - удалить

   GET  /api/v1/routes/saved - сохраненные маршруты
   POST /api/v1/routes/saved - сохранить маршрут

2. АВТОРИЗАЦИЯ:
   - JWT токены (используй существующий auth.py)
   - API key для партнеров

3. ВЕРСИОНИРОВАНИЕ:
   - /api/v1/, /api/v2/ - semantic versioning

Добавь в routers/ и services/
Документируй в docs/api/navigation-api.md
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.9: WEBSOCKET ДЛЯ REAL-TIME НАВИГАЦИИ
─────────────────────────────────────────────────────────────────────────────

Задача: Создай WebSocket API для real-time обновлений.

Требования:

1. СОЕДИНЕНИЯ:
   - ws://server/nav/{session_id} - навигация
   - ws://server/tracking/{device_id} - отслеживание

2. СООБЩЕНИЯ (JSON):
   - Client -> Server:
     {"type": "position_update", "lat": 55.75, "lng": 37.61, "accuracy": 10}
     {"type": "start_navigation", "route_id": "..."}
     {"type": "request_recalc"}
   
   - Server -> Client:
     {"type": "instruction", "maneuver": "turn_left", "distance": 500}
     {"type": "eta_update", "minutes": 15}
     {"type": "traffic_ahead", "delay": 300}
     {"type": "route_changed", "new_eta": ...}

3. АВТОРИЗАЦИЯ:
   - JWT в query параметре или первом сообщении
   - Heartbeat каждые 30 секунд

Интегрируй с существующим ai_engine/agent/websocket_server.py
Создай专门的 WebSocket router
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.10: GRPC ДЛЯ MICROSERVICES
─────────────────────────────────────────────────────────────────────────────

Задача: Настрой gRPC для межсервисной коммуникации.

Требования:

1. PROTO DEFINITIONS:
   - navigation.proto: StartNavigation, UpdatePosition, GetInstruction
   - routing.proto: CalculateRoute, GetMatrix, OptimizeWaypoints
   - location.proto: TrackDevice, GetHistory, CreateGeofence

2. ИНСТРУМЕНТЫ:
   - grpcio-tools для генерации
   - grpcio для Python
   - Swift protoc-gen-swift для iOS

3. КОНФИГУРАЦИЯ:
   - Docker compose для dev
   - Kubernetes для prod
   - Load balancing

Создай proto/ директорию
Сгенерируй Python и TypeScript клиенты
Добавь gRPC сервер в main.py
```

### День 11-14: Клиентская инфраструктура

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.11: НАСТРОЙКА REACT NATIVE ДЛЯ МОБИЛЬНОГО ПРИЛОЖЕНИЯ
─────────────────────────────────────────────────────────────────────────────

Задача: Настрой React Native проект для навигатора.

Требования:

1. СТРУКТУРА ПРОЕКТА:
   - npx react-native init MansoniMap
   - TypeScript по умолчанию
   - React Navigation для routing

2. НАВИГАЦИЯ (react-navigation):
   - Stack Navigator: Map -> Search -> Route -> Navigate
   - Bottom Tab: Map, Search, Saved, Profile

3. ЗАВИСИМОСТИ:
   - react-native-maps (или Mapbox)
   - @react-native-community/geolocation
   - @react-native-community/async-storage
   - react-native-sensors (гироскоп, акселерометр)

4. CI/CD:
   - iOS через Fastlane
   - Android через Gradle

Создай apps/mobile/
Добавь .github/workflows/mobile-*.yml
Документируй в docs/mobile/setup.md
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.12: НАСТРОЙКА CAPACITOR ДЛЯ ГИБРИДНОГО ПРИЛОЖЕНИЯ
─────────────────────────────────────────────────────────────────────────────

Задача: Настрой Capacitor (существующий в проекте) для навигатора.

Требования:

1. ИСПОЛЬЗУЙ СУЩЕСТВУЮЩИЙ:
   - apps/mobile-shell/ - существующий skeleton
   - capacitor.config.ts - существующая конфигурация

2. НАСТРОЙ ПЛАГИНЫ:
   - @capacitor/geolocation - GPS
   - @capacitor/push-notifications - push
   - @capacitor/device - информация об устройстве
   - @capacitor/storage - локальное хранилище

3. WEB VIEW:
   - Создай карту в WebView для быстрой итерации
   - Используй react-leaflet или maplibre-gl-js

Обнови apps/mobile-shell/
Добавь нативные плагины для Android/iOS
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 0.13: PWA ВЕРСИЯ ДЛЯ WEB
─────────────────────────────────────────────────────────────────────────────

Задача: Создай PWA версию навигатора.

Требования:

1. ТЕХНОЛОГИИ:
   - Vue.js 3 или React 18
   - Vite для сборки
   - MapLibre GL JS для карты (open source)

2. PWA FEATURES:
   - Service Worker для offline
   - Web App Manifest
   - Add to Home Screen
   - Push Notifications

3. ФУНКЦИОНАЛ:
   - Карта с маркерами
   - Поиск адресов
   - Построение маршрута
   - Навигация (упрощенная)

Созди apps/web-pwa/
Добавь vite.config.ts с PWA плагинами
Документируй требования в docs/web/pwa.md
```

---

# PHASE 1: ОСНОВНОЙ ДВИЖОК (Дни 15-45)

## Неделя 3: Карта и рендеринг

### День 15-18: Базовый рендеринг

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.1: СОЗДАНИЕ МЕНЕДЖЕРА ТАЙЛОВ
─────────────────────────────────────────────────────────────────────────────

Задача: Создай систему загрузки и кэширования картографических тайлов.

Требования:

1. ИСТОЧНИКИ ДАННЫХ:
   - OpenStreetMap (бесплатный)
   - Mapbox (есть API key)
   - Собственные тайлы (сгенерировать из OSM)

2. ЗАГРУЗКА:
   - Tile URL template: https://tile.openstreetmap.org/{z}/{x}/{y}.png
   - Range: zoom 1-18, bounds worldwide
   - Параллельная загрузка (до 6 одновременно)

3. КЭШИРОВАНИЕ:
   - LRU кэш в памяти (100MB)
   - Disk кэш для офлайн (IndexedDB)
   - Предзагрузка соседних тайлов

4. КАЧЕСТВО:
   - WebP с fallback на PNG
   - Progressive loading

Создай utils/tile-manager.ts
Интегрируй с картой
Добавь тесты производительности
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.2: ИНТЕГРАЦИЯ MAPLIBRE GL JS
─────────────────────────────────────────────────────────────────────────────

Задача: Интегрируй MapLibre GL JS для рендеринга карты.

Требования:

1. БАЗОВАЯ КАРТА:
   - Инициализация карты с центром на Москве
   - Управление зумом (1-18)
   - Панорамирование и вращение
   - Gesture recognition (pinch, rotate)

2. СТИЛИ:
   - Используй светлую/темную тему
   - Кастомизация стиля через MapLibre Style Spec

3. ПОДДЕРЖКА:
   - WebGL рендеринг
   - GPU ускорение
   - Canvas для fallback

4. СЛОИ:
   - Base layer (тайлы)
   - Marker layer (POI)
   - Route layer (маршрут)
   - User location layer

Создай components/Map.tsx
Добавь в навигацию React
Документируй API в docs/components/Map.md
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.3: СЛОИ И МАРКЕРЫ НА КАРТЕ
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй отображение POI и маршрутов на карте.

Требования:

1. POI МАРКЕРЫ:
   - Разные иконки для категорий (еда, парковка, заправка)
   - Cluster markers при зуме < 14
   - Плавающие кнопки категорий

2. МАРШРУТ:
   - Polyline с разными цветами для типов дорог
   - Стрелки направления
   - Альтернативные маршруты (серый цвет)
   - Анимация прохождения маршрута

3. ПОЛЬЗОВАТЕЛЬ:
   - Синяя точка с точностью (круг)
   - Направление (треугольник)
   - Путь (пройденный трек)

4. ДРУГИЕ:
   - Геозоны (полигоны)
   - Трафик (цветные линии)
   - 3D здания (опционально)

Расширь компонент Map.tsx
Создай components/MapLayers.tsx
Добавь кастомные стили в assets/map-styles/
```

### День 19-21: Позиционирование

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.4: GPS ПОЗИЦИОНИРОВАНИЕ
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй точное определение местоположения.

Требования:

1. GPS (Geolocation API):
   - Запрос position с high accuracy
   - Watch position для трекинга
   - Обработка ошибок (timeout, permission)

2. СЕНСОРЫ:
   - Акселерометр - детекция движения
   - Гироскоп - направление
   - Магнетометр - компас

3. АЛГОРИТМЫ:
   - Kalman filter для сглаживания
   - Dead reckoning в туннелях
   - Snap to road (привязка к дороге)

4. ACCURACY:
   - Показывать точность визуально
   - Фильтровать неточные позиции (>100m)

Создай hooks/useLocation.ts
Создай services/location-provider.ts
Интегрируй с Map компонентом
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.5:ГИБРИДНОЕ ПОЗИЦИОНИРОВАНИЕ
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй позиционирование без GPS (WiFi, Cell).

Требования:

1. WIFI POSITIONING:
   - Сканирование доступных WiFi сетей
   - Запрос к сервису геолокации (Mozilla/Google)
   - Fallback если GPS недоступен

2. CELL TOWER POSITIONING:
   - Получение ID ближайших вышек
   - Triangulation (минимум 3 вышки)
   - MCELL, LAC для точности

3. INDOOR POSITIONING:
   - Bluetooth beacons (если есть)
   - WiFi fingerprinting
   - Floor detection (акселерометр)

4. ОПТИМИЗАЦИЯ:
   - Battery saver режим (редкие обновления)
   - High accuracy режим (по запросу)

Создай services/hybrid-location.ts
Добавь native модули для WiFi/Cell
Документируй требования к permissions
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.6: Kalman Filter ДЛЯ ЛОКАЦИИ
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй Kalman filter для улучшения точности позиционирования.

Требования:

1. МОДЕЛЬ:
   - State: [lat, lng, vx, vy] (позиция + скорость)
   - Measurement: GPS позиция, датчики
   - Process noise: движение транспорта

2. ИНТЕГРАЦИЯ:
   - Предсказание при обновлении датчиков
   - Коррекция при получении GPS
   - Сброс при резком скачке (>100m)

3. УЛУЧШЕНИЯ:
   - Учет дорожной сети (Map matching)
   - Учет скорости транспорта
   - Фильтрация выбросов

Создай utils/kalman-filter.ts
Интегрируй с useLocation
Добавь тесты на реальных данных
```

### День 22-24: Поиск и геокодинг

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.7: ИНТЕГРАЦИЯ ПОИСКА POI
─────────────────────────────────────────────────────────────────────────────

Задача: Интегрируй поиск POI в мобильное приложение.

Требования:

1. API ИНТЕГРАЦИЯ:
   - Используй существующий poi_service.py
   - Запросы к /api/v1/poi/search
   - Кэширование результатов

2. UI:
   - Search bar с автодополнением
   - История поиска
   - Избранные места
   - Категории (недавно, часто, категории)

3. ФИЛЬТРЫ:
   - По категории (еда, отели, парковка...)
   - По расстоянию
   - По рейтингу
   - Открыто сейчас

4. РЕЗУЛЬТАТЫ:
   - Список с иконками и расстоянием
   - Миникарточки при скролле
   - Кластеризация на карте

Создай components/SearchBar.tsx
Создай screens/SearchScreen.tsx
Интегрируй с API
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.8: ГЕОКОДИНГ (АДРЕС <-> КООРДИНАТЫ)
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй прямое и обратное геокодирование.

Требования:

1. ПРЯМОЕ (адрес -> координаты):
   - Используй существующий geocoding_service.py
   - Поддержка российских адресов
   - Подсказки при вводе (autocomplete)

2. ОБРАТНОЕ (координаты -> адрес):
   - What3Words альтернатива
   - Улица + дом + город
   - Область/регион

3. ОСОБЕННОСТИ:
   - Обработка ошибок
   - Fallback на Nominatim (OSM)
   - Кэширование популярных адресов

4. UI:
   - "Показать на карте" из адреса
   - "Мой адрес" из точки на карте
   - Валидация ввода

Создай services/geocoding-client.ts
Создай hooks/useGeocoding.ts
Интегрируй в компоненты
```

## Неделя 4: Маршрутизация и навигация

### День 25-28: Построение маршрутов

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.9: ИНТЕГРАЦИЯ МАРШРУТИЗАЦИИ В КЛИЕНТ
─────────────────────────────────────────────────────────────────────────────

Задача: Интегрируй построение маршрутов в мобильное приложение.

Требования:

1. API ВЫЗОВ:
   - Используй routing_service.py
   - POST /api/v1/routes/calculate
   - Поддержка нескольких профилей (auto, pedestrian, bicycle)

2. ПАРАМЕТРЫ:
   - Избегание (платные дороги, паромы, грунтовые)
   - Время отправления
   - Альтернативные маршруты

3. ОТОБРАЖЕНИЕ:
   - Выбор маршрута из списка
   - Предпросмотр на карте
   - Информация: расстояние, время, пробки

4. КЭШИРОВАНИЕ:
   - Кэш построенных маршрутов
   - Offline маршруты (если есть данные)

Создай hooks/useRouting.ts
Создай components/RoutePreview.tsx
Интегрируй с Map
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.10: TURN-BY-TURN НАВИГАЦИЯ
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй полноценную turn-by-turn навигацию.

Требования:

1. МАНЕВРЫ:
   - Типы: turn, merge, depart, arrive, continue, U-turn
   - Улицы и номера домов
   - Дистанция до маневра
   - Визуальная картинка (стрелка)

2. ОБНОВЛЕНИЯ:
   - Приближение к маневу (< 200m)
   - Пересчет при отклонении (> 50m)
   - Новые пробки

3. АНИМАЦИЯ:
   - Плавное движение по маршруту
   - Камера следит за пользователем
   - Автоматическое вращение

4. ГОЛОС:
   - TTS инструкции (используй Web Speech API)
   - Озвучкаstreet name
   - Настройка громкости

Создай services/navigation-engine.ts
Создай components/NavigationView.tsx
Создай hooks/useNavigation.ts
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.11: ПЕРЕСЧЕТ МАРШРУТА ПРИ ОТКЛОНЕНИИ
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй автоматический пересчет маршрута.

Требования:

1. ДЕТЕКЦИЯ ОТКЛОНЕНИЯ:
   - Расстояние от маршрута > 30m
   - Движение в неправильном направлении
   - Пропуск поворота

2. АЛГОРИТМ:
   - Quick recalc (локальный)
   - Full recalc (весь маршрут)
   - Rerouting к новому маршруту

3. ПОЛЬЗОВАТЕЛЮ:
   - Сообщение "Пересчитываем маршрут"
   - Минимальное время пересчета (<1 сек)
   - Избегание "спиральных" маршрутов

4. ЛОГИРОВАНИЕ:
   - Track отклонений для улучшения
   - Анализ ложных срабатываний

Расширь navigation-engine.ts
Добавь тесты для edge cases
```

### День 29-32: Дополнительные функции навигации

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.12: ГОЛОСОВЫЕ ПОДСКАЗКИ (TTS)
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй голосовые инструкции для навигации.

Требования:

1. TTS ENGINE:
   - Web Speech API (браузер)
   - iOS AVSpeechSynthesizer
   - Android TextToSpeech

2. ИНСТРУКЦИИ:
   - "Через 500 метров поверните направо"
   - "На следующем перекрестке налево"
   - "Вы приехали"
   - Названия улиц

3. НАСТРОЙКИ:
   - Громкость относительно музыки
   - Язык голоса
   - Мужской/женский голос
   - Скорость речи

4. ОПТИМИЗАЦИЯ:
   - Предварительная генерация
   - Кэш аудио
   - Очередь команд

Создай services/voice-guidance.ts
Интегрируй с navigation-engine
Создай настройки в UI
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.13: TRAFFIC ИНТЕГРАЦИЯ
─────────────────────────────────────────────────────────────────────────────

Задача: Интегрируй данные о пробках в навигацию.

Требования:

1. ДАННЫЕ:
   - Используй traffic_service.py
   - Пробки на маршруте
   - Исторические паттерны
   - Аварии и ремонты

2. ОТОБРАЖЕНИЕ:
   - Цветовая схема дорог (зеленый-желтый-красный)
   - Предупреждения на маршруте
   - Задержка времени

3. ПЕРЕСЧЕТ:
   - Автоматический при высоких пробках
   - Предложение альтернативы
   - Учет в ETA

4. ИСТОЧНИКИ:
   - Яндекс.Пробки (API)
   - OpenTraffic (OSM)
   - Собственные данные (crowdsource)

Интегрируй с routing_service
Покажи на карте слоем
Документируй источники
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 1.14: РАЗЛИЧНЫЕ ТИПЫ НАВИГАЦИИ
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй навигацию для разных видов транспорта.

Требования:

1. АВТОМОБИЛЬ:
   - Быстрый/короткий/экономичный маршрут
   - Ограничения для грузовиков (габариты)
   - Платные дороги
   - Паромы

2. ПЕШЕХОД:
   - Тротуары и пешеходные переходы
   - Парки и скверы
   - Лестницы (можно/нельзя)
   - Пешеходные зоны

3. ВЕЛОСИПЕД:
   - Велодорожки
   - Опасные участки
   - Высота (холмы)
   - Парковки для велосипедов

4. ОБЩЕСТВЕННЫЙ ТРАНСПОРТ:
   - Автобус, метро, трамвай
   - Пересадки
   - Расписание
   - Первый/последний вагон

Расширь routing_service
Создай профили в UI
Добавь специфические функции
```

---

# PHASE 2: ДИНАМИЧЕСКИЙ UI (Дни 46-75)

## Неделя 5: C++ движок (основы)

### День 33-38: C++ архитектура

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 2.1: ПРОЕКТИРОВАНИЕ C++ ДВИЖКА
─────────────────────────────────────────────────────────────────────────────

Задача: Спроектируй C++ движок для навигатора (аналог Amap).

Требования:

1. АРХИТЕКТУРА:
   ├── core/              # Ядро
   │   ├── memory/       # Управление памятью
   │   ├── thread/      # Потоки
   │   └── utils/       # Утилиты
   ├── rendering/       # Рендеринг
   │   ├── opengl/      # OpenGL ES
   │   ├── shaders/     # Шейдеры
   │   └── textures/    # Текстуры
   ├── map/             # Карта
   │   ├── tiles/       # Тайлы
   │   ├── layers/      # Слои
   │   └── style/       # Стили
   ├── navigation/      # Навигация
   │   ├── routing/     # Маршруты
   │   └── guidance/     # Команды
   └── platform/         # Платформа
       ├── android/     # Android NDK
       └── ios/         # iOS

2. СБОРКА:
   - CMake для кросс-компиляции
   - Android NDK toolchain
   - iOS Xcode toolchain

3. ИНТЕГРАЦИЯ JNI/OBJC:
   - Android: JNI вызовы из Java
   - iOS: C++ class обертка для Swift

Создай engine/ директорию
Напиши CMakeLists.txt
Документируй архитектуру
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 2.2: OpenGL ES РЕНДЕРЕР
─────────────────────────────────────────────────────────────────────────────

Задача: Создай OpenGL ES рендерер для карты.

Требования:

1. SHADERS:
   - Vertex shader: позиционирование, текстуры
   - Fragment shader: освещение, альфа-смешивание
   - Атрибуты: position, texcoord, color

2. RENDERING PIPELINE:
   - Очистка буфера
   - Отрисовка тайлов (quads)
   - Наложение маркеров
   - Наложение маршрута (lines)

3. ОПТИМИЗАЦИИ:
   - Frustum culling (не рисовать вне экрана)
   - Level of detail (LOD)
   - Instanced rendering для маркеров
   - Double buffering

4. TEXTURES:
   - Загрузка тайлов как текстур
   - Mipmapping
   - Atlas для иконок

Создай engine/rendering/opengl-*
Добавь шейдеры в assets/shaders/
Протестируй на Android/iOS
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 2.3: ТАЙЛОВЫЙ МЕНЕДЖЕР
─────────────────────────────────────────────────────────────────────────────

Задача: Создай менеджер тайлов в C++.

Требования:

1. ЗАГРУЗКА:
   - Async загрузка в отдельном потоке
   - Приоритет: видимые > соседние > предсказанные
   - Кэш в памяти (50MB)
   - Кэш на диске (офлайн)

2. ФОРМАТЫ:
   - PNG/JPEG для растровых
   - MVT (Mapbox Vector Tile) для векторных

3. ДЕКОДИРОВАНИЕ:
   - zlib для сжатия
   - ProtoBuf для векторных тайлов

4. ПРЕДЗАГРУЗКА:
   - По маршруту навигации
   - По направлению движения

Создай engine/map/tiles/*
Интегрируй с rendering
Добавь кэширование
```

### День 39-42: Virtual DOM

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 2.4: VIRTUAL DOM ДЛЯ UI
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй Virtual DOM движок для UI (аналог Amap).

Требования:

1. АРХИТЕКТУРА (参考 Amap):
   JS (React-like) → Virtual DOM → C++ Engine → Native UI

2. КОМПОНЕНТЫ:
   - JSEngine: JavaScript runtime (V8 или JavaScriptCore)
   - VDOMParser: парсит JSX в виртуальное дерево
   - LayoutEngine: вычисляет позиции (CSS-like)
   - DiffEngine: сравнивает деревья
   - NativeBridge: вызывает нативный UI

3. CSS-LIKE STYLES:
   - flexbox layout
   - position: absolute/relative
   - width, height, margin, padding
   - colors, borders, shadows

4. ИНТЕГРАЦИЯ:
   - Android: View создаются через Java
   - iOS: UIView создаются через Swift

Создай engine/ui/vdom/*
Создай интеграцию с React Native
Документируй API
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 2.5: LAYOUT ENGINE
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй движок компоновки (CSS-like).

Требования:

1. LAYOUT MODES:
   - Block (вертикальный стек)
   - Flexbox (основной)
   - Absolute (позиционирование)

2. FLEXBOX SUPPORT:
   - flex-direction: row/column
   - justify-content: start/center/end...
   - align-items: stretch/start/center...
   - flex-wrap: wrap/nowrap
   - flex: 1 для растяжения

3. TEXT LAYOUT:
   - Измерение текста
   - Text wrapping
   - Line height, font size
   - Text align

4. PERFORMANCE:
   - Кэш измерений
   - Incremental layout (только изменения)
   - Background thread

Создай engine/ui/layout/*
Интегрируй с VDOM
Тестируй на сложных макетах
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 2.6: NATIVE UI КОМПОНЕНТЫ
─────────────────────────────────────────────────────────────────────────────

Задача: Создай нативные UI компоненты.

Требования:

1. БАЗОВЫЕ КОМПОНЕНТЫ:
   - View (контейнер)
   - Text (текст с стилями)
   - Image (картинка)
   - Button (кнопка)
   - ScrollView (скролл)
   - ListView (список с виртуализацией)

2. MAP UI:
   - Search bar
   - Bottom sheet (результаты)
   - Turn-by-turn карточки
   - ETA display

3. ANIMATIONS:
   - Transition animations
   - Gesture animations
   - Linked animations (связанные)

4. STYLE SYSTEM:
   - Темы (light/dark)
   - CSS-like классы
   - Наследование стилей

Создай native/ui/android/*
Создай native/ui/ios/*
Интегрируй с VDOM
```

## Неделя 6: Динамическая генерация

### День 43-48: JS -> Native

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 2.7: JAVASCRIPT RUNTIME ИНТЕГРАЦИЯ
─────────────────────────────────────────────────────────────────────────────

Задача: Интегрируй JavaScript движок в C++ движок.

Требования:

1. JS ENGINES:
   - Android: V8 (Chrome) или Hermes (Facebook)
   - iOS: JavaScriptCore (встроенный)
   - Universal: duktape (lightweight)

2. BINDINGS:
   - Expose C++ функции в JS
   - Call JS функции из C++
   - Async calls с callbacks

3. MODULE SYSTEM:
   - require() для модулей
   - npm package compatibility
   - Sandboxed execution

4. DEBUGGING:
   - Chrome DevTools protocol
   - Source maps
   - Breakpoints

Создай engine/js/runtime-*
Добавь базовые модули
Настрой debugging
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 2.8: ДИНАМИЧЕСКАЯ ЗАГРУЗКА UI
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй загрузку UI из JS.

Требования:

1. CODE LOADING:
   - Fetch JS bundle по URL
   - Локальный кэш
   - Version checking

2. RENDERING:
   - JSX → Virtual DOM
   - Diff → Native components
   - Mount на нативную платформу

3. UPDATES:
   - Hot reload (без перезагрузки)
   - A/B testing компонентов
   - Feature flags

4. OFFLINE:
   - Bundle в офлайн-пакете
   - Fallback UI если не загружено

Создай system/dynamic-loader.ts
Интегрируй с VDOM
Добавь кэширование
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 2.9: АНИМАЦИИ И ЖЕСТЫ
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй анимации и жесты в UI.

Требования:

1. ANIMATION TYPES:
   - Transitions (переходы между экранами)
   - Gestures (drag, pinch, rotate)
   - Linked (связанные - спидометр + маршрут)
   - Physics-based (spring, decay)

2. IMPLEMENTATION:
   - C++ Animation engine
   - vsync-synced rendering
   - 60fps target

3. GESTURE RECOGNITION:
   - Pan (движение)
   - Pinch (масштаб)
   - Rotate (поворот)
   - Tap (тап)
   - Long press (долгое нажатие)

4. UI COMPONENTS:
   - Sliding panels
   - Bottom sheets
   - Map gestures

Создай engine/ui/animation/*
Создай engine/ui/gesture/*
Интегрируй с Native UI
```

### День 49-52: Стили и темы

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 2.10: STYLE SYSTEM
─────────────────────────────────────────────────────────────────────────────

Задача: Создай систему стилей для UI.

Требования:

1. CSS-LIKE SYNTAX:
   - Поддержка основных CSS свойств
   - Media queries (размеры экрана)
   - Pseudo-selectors (:hover, :active)

2. THEMES:
   - Light theme
   - Dark theme (системный выбор)
   - Custom themes (пользовательские)

3. DESIGN TOKENS:
   - Цвета (primary, secondary, etc)
   - Spacing (xs, sm, md, lg, xl)
   - Typography (font sizes, weights)
   - Shadows, borders

4. PERFORMANCE:
   - Stylesheet parsing (при загрузке)
   - Style computation caching
   - Incremental updates

Создай engine/ui/styles/*
Создай assets/themes/
Интегрируй с Layout Engine
```

---

# PHASE 3: ОФЛАЙН РЕЖИМ (Дни 76-105)

## Неделя 7: Офлайн данные

### День 53-58: Скачивание карт

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 3.1: СИСТЕМА ОФЛАЙН КАРТ
─────────────────────────────────────────────────────────────────────────────

Задача: Создай полноценную систему офлайн карт.

Требования:

1. РЕГИОНЫ:
   - Страны (Россия, СНГ, Европа)
   - Области/регионы
   - Города
   - Custom (выбрать область на карте)

2. ДАННЫЕ:
   - Тайлы (все zoom levels 10-16)
   - POI база (отели, рестораны, АЗС...)
   - Дорожный граф (для маршрутов)
   - Геокодинг данные

3. РАЗМЕР:
   - Сжатие (LZ4 для тайлов)
   - Пакетирование (по регионам)
   - Размер типичного региона: 50-200MB

4. API:
   - List available regions
   - Download region
   - Delete region
   - Update region

Созди services/offline-maps.ts
Создай API в backend
Документируй формат данных
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 3.2: МЕНЕДЖЕР ЗАГРУЗОК
─────────────────────────────────────────────────────────────────────────────

Задача: Создай менеджер загрузки офлайн данных.

Требования:

1. DOWNLOAD MANAGER:
   - Очередь загрузок
   - Pause/resume
   - Background download
   - WiFi-only опция

2. PROGRESS:
   - Progress bar с %
   - Estimated time
   - Pause/resume кнопки
   - Автоматическое продолжение

3. STORAGE:
   - Проверка места на устройстве
   - Удаление старых регионов
   - Квота на storage

4. VERIFICATION:
   - Checksum после загрузки
   - Проверка целостности
   - Retry при ошибках

Создай services/download-manager.ts
Интегрируй с offline maps
Добавь UI прогресс
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 3.3: ОФЛАЙН МАРШРУТЫ
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй построение маршрутов офлайн.

Требования:

1. ROUTING OFFLINE:
   - Используй локальный дорожный граф
   - Edge expansion из OSM данных
   - Valhalla offline mode (если возможно)

2. ОГРАНИЧЕНИЯ:
   - Только основные дороги
   - Нет пробок (исторические данные)
   - Нет live updates

3. КАЧЕСТВО:
   - Достаточно для навигации
   - Уведомление о офлайн режиме

4. СИНХРОНИЗАЦИЯ:
   - Обновление графа (раз в месяц)
   -增量ные обновления

Создай offline-routing-engine.ts
Интегрируй с routing service
Добавь UI indicator
```

### День 59-63: Работа офлайн

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 3.4: СИНХРОНИЗАЦИЯ OFFLINE/ONLINE
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй синхронизацию между офлайн и онлайн.

Требования:

1. КОГДА ОНЛАЙН:
   - Загрузить тайлы которые были запрошены
   - Обновить POI данные
   - Синхронизировать избранное и историю
   - Загрузить обновления маршрутов

2. КОГДА ОФЛАЙН:
   - Использовать локальные данные
   - Queue изменений (избранное, история)
   - Sync когда появится интернет

3. CONFLICTS:
   - Timestamp-based resolution
   - User notification о конфликтах

4. SETTINGS:
   - Auto-sync on WiFi
   - Manual sync option

Создай services/sync-manager.ts
Интегрируй с offline maps
Добавь UI настройки
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 3.5: PWA OFFLINE SUPPORT
─────────────────────────────────────────────────────────────────────────────

Задача: Добавь offline поддержку в PWA версию.

Требования:

1. SERVICE WORKER:
   - Cache-first для тайлов
   - Network-first для API
   - Background sync для данных

2. СТРАТЕГИИ:
   - Precache основных ресурсов
   - Runtime caching для тайлов
   - Offline fallback страница

3. DATA:
   - IndexedDB для локальных данных
   - Sync очереди
   - Offline maps (упрощенные)

4. STORAGE MANAGEMENT:
   - Quota API для лимитов
   - User notification очистки
   - Auto-cleanup старых данных

Обнови PWA для офлайн
Добавь Service Worker стратегии
Протестируй в Chrome DevTools
```

---

# PHASE 4: ПРОИЗВОДИТЕЛЬНОСТЬ (Дни 106-135)

## Неделя 8: Оптимизация

### День 64-70: Производительность

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 4.1: PERFORMANCE OPTIMIZATION - STARTUP
─────────────────────────────────────────────────────────────────────────────

Задача: Оптимизируй время старта приложения.

Требования:

1. CRITICAL PATH:
   - Lazy load не критичных модулей
   - Параллельная загрузка ресурсов
   - skeleton screens пока грузится

2. BUNDLE SPLITTING:
   - Разделение на чанки
   - Code splitting по маршрутам
   - Preload предсказанных чанков

3. ASSETS:
   - Сжатие картинок (WebP)
   - Инлайн критичных CSS
   - Бандлирование шрифтов

4. NATIVE:
   - Minimal native dependencies
   - Hermes на Android (уменьшает startup)
   - Pre-compiled JS (JSC on iOS)

Измерь startup time (Android Studio / Instruments)
Оптимизируй бандлы
Добавь lazy loading
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 4.2: PERFORMANCE OPTIMIZATION - MAP RENDERING
─────────────────────────────────────────────────────────────────────────────

Задача: Оптимизируй рендеринг карты до 60 FPS.

Требования:

1. RENDERING:
   - 60 FPS цель (16ms на кадр)
   - GPU рендеринг (hardware acceleration)
   - Texture atlas для иконок

2. CULLING:
   - Frustum culling (за экраном)
   - Distance culling (далекие объекты)
   - LOD (level of detail)

3. THREADING:
   - Background tile decoding
   - Main thread только rendering
   - Web Workers для вычислений

4. MEMORY:
   - Recycler для маркеров
   - Bitmap reuse
   - Monitor memory pressure

Профилируй в Chrome DevTools
Оптимизируй рендер
Добавь adaptive quality
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 4.3: MEMORY OPTIMIZATION
─────────────────────────────────────────────────────────────────────────────

Задача: Оптимизируй использование памяти.

Требования:

1. JAVASCRIPT:
   - Object pooling для маркеров
   - WeakMap/WeakSet для cached objects
   - Closure leak detection

2. NATIVE:
   - Native memory tracking
   - Bitmap recycling
   - Leak detection (Android Studio / Instruments)

3. MAP:
   - Tile cache с LRU eviction
   - Polygon/polyline simplification
   - Text glyph caching

4. THRESHOLDS:
   - iOS: < 150MB peak
   - Android: < 200MB peak

Профилируй с Instruments
Исправь memory leaks
Добавь monitoring
```

### День 71-75: Мониторинг

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 4.4: PERFORMANCE MONITORING
─────────────────────────────────────────────────────────────────────────────

Задача: Настрой мониторинг производительности.

Требования:

1. METRICS:
   - App launch time (cold/warm)
   - Frame rate (FPS)
   - Memory usage (JS heap, native)
   - Network requests
   - API latency

2. COLLECTION:
   - Background collection
   - Sample-based (не каждый раз)
   - Low battery impact

3. REPORTING:
   - Crash reporting (Firebase / Sentry)
   - ANR detection
   - Custom events

4. DASHBOARD:
   - Real-time dashboard
   - Filter by OS, version, device
   - Alerting на аномалии

Интегрируй Firebase Performance / Sentry
Настрой dashboard
Добавь alerting rules
```

---

# PHASE 5: КРОСС-ПЛАТФОРМА (Дни 136-165)

## Неделя 9-10: Нативные приложения

### День 76-85: iOS приложение

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 5.1: IOS ПРИЛОЖЕНИЕ - SwiftUI
─────────────────────────────────────────────────────────────────────────────

Задача: Создай нативное iOS приложение на SwiftUI.

Требования:

1. APP STRUCTURE:
   - SwiftUI views + UIKit интеграция
   - Navigation stack
   - Tab bar navigation

2. MAP:
   - MapKit integration (или MapLibre)
   - Custom annotations
   - Overlays для маршрута

3. CORE:
   - Location services
   - Background location
   - Push notifications (APNs)

4. UI COMPONENTS:
   - Search interface
   - Route preview
   - Navigation view
   - Settings

5. PERF:
   - SwiftUI optimized
   - Lazy loading
   - Image caching

Созди apps/ios/
Добавь в Xcode project
Добавь GitHub Actions для сборки
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 5.2: IOS - CARPLAY SUPPORT
─────────────────────────────────────────────────────────────────────────────

Задача: Добавь поддержку CarPlay.

Требования:

1. CARPLAY FRAMEWORK:
   - CPTemplateApplicationSceneDelegate
   - CarPlay scene lifecycle
   - Navigation template

2. UI:
   - Tab bar в CarPlay
   - Turn-by-turn display
   - Search templates
   - List templates

3. MAP:
   - MapKit for CarPlay
   - Auto routing
   - Voice guidance через CarPlay audio

4. LIMITATIONS:
   - Limited UI controls
   - Focus on driving safety
   - Audio-centric interaction

Добавь CarPlay scene в iOS app
Создай CarPlay UI
Документируй требования Apple
```

### День 86-95: Android приложение

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 5.3: ANDROID ПРИЛОЖЕНИЕ - Kotlin
─────────────────────────────────────────────────────────────────────────────

Задача: Создай нативное Android приложение.

Требования:

1. ARCHITECTURE:
   - Kotlin + Jetpack Compose
   - MVVM + Clean Architecture
   - Hilt для DI

2. MAP:
   - MapLibre Android SDK
   - Custom styles
   - Offline maps support

3. CORE:
   - Fused Location Provider
   - WorkManager для background
   - Firebase Cloud Messaging

4. FEATURES:
   - All features from React Native
   - Widget (home screen)
   - Android Auto support

5. PERF:
   - ProGuard/R8 optimization
   - Baseline profiles
   - Splash screen optimization

Созди apps/android/
Добавь Gradle build
Добавь CI/CD
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 5.4: ANDROID - ANDROID AUTO
─────────────────────────────────────────────────────────────────────────────

Задача: Добавь поддержку Android Auto.

Требования:

1. CAR LIBRARY:
   - CarAppService
   - Screen lifecycle
   - Navigation template

2. UI:
   - Simple list templates
   - Navigation templates
   - Message templates

3. NAVIGATION:
   - NavigationTemplate для guidance
   - PlaceListMapTemplate для поиска
   - Audio focus для voice

4. RESTRICTIONS:
   - Minimize driver distraction
   - Limited screen time
   - Voice-first interaction

Добавь Android Auto module
Создай Auto screens
Документируй требования Google
```

---

# PHASE 6: ДОПОЛНИТЕЛЬНЫЕ ФУНКЦИИ (Дни 166-200)

## Неделя 11-12: AR и интеграции

### День 96-105: AR навигация

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 6.1: AR НАВИГАЦИЯ
─────────────────────────────────────────────────────────────────────────────

Задача: Реализуй AR навигацию (augmented reality).

Требования:

1. CAMERA:
   - Camera2 API / AVFoundation
   - AR session management
   - Frame processing

2. COMPUTER VISION:
   - Road plane detection
   - Building recognition
   - Lane detection

3. OVERLAY:
   - AR arrows на камере
   - Street name labels
   - Distance to turn
   - Speed limit display

4. UX:
   - Auto brightness
   - Stabilization
   - Low light mode

Созди features/ar-navigation/
Интегрируй с камерой
Добавь в нативные apps
```

```
─────────────────────────────────────────────────────────────────────────────
ПРОМТ 6.2: ГОЛОСОВОЙ АССИСТЕНТ
─────────────────────────────────────────────────────────────────────────────

Задача: Интегрируй голосовых ассистентов.

Требования:

1. ANDROID:
   - Google Assistant integration
   - App Actions
   - Voice shortcuts

2. IOS:
   - Siri integration
   - Shortcuts app
   - SiriKit

3. VOICE COMMANDS:
   - "Navigate to [address]"
   - "Where am I?"
   - "Show traffic"
   - "Find nearby [category]"

4. Custom:
   - In-app voice commands
   - Keyword detection
   - Natural language

Интегрируй с Siri/Google Assistant
Добавь voice commands в нативные apps
```

---

# 📊 СВОДКА ПО ФАЗАМ

| Phase | Days | Tasks | Key Deliverables |
|-------|------|-------|------------------|
| **Phase 0** | 1-14 | 13 промтов | Инфраструктура, CI/CD, API |
| **Phase 1** | 15-45 | 20 промтов | Карта, маршрутизация, навигация |
| **Phase 2** | 46-75 | 10 промтов | C++ движок, Virtual DOM, UI |
| **Phase 3** | 76-105 | 5 промтов | Офлайн режим |
| **Phase 4** | 106-135 | 4 промта | Оптимизация |
| **Phase 5** | 136-165 | 4 промта | iOS/Android нативные |
| **Phase 6** | 166-200 | 2 промта | AR, голосовые ассистенты |

---

# 🎯 ПРИОРИТЕТЫ РЕАЛИЗАЦИИ

## Top 10 критических задач (начни с этих):

1. **Промт 0.11** - Настройка React Native (это твой UI)
2. **Промт 1.2** - MapLibre GL JS интеграция (визуализация)
3. **Промт 1.4** - GPS позиционирование (базовая функция)
4. **Промт 1.9** - Интеграция маршрутизации (核心 функция)
5. **Промт 1.10** - Turn-by-turn навигация (главная фича)
6. **Промт 1.12** - Голосовые подсказки (USPs)
7. **Промт 3.1** - Офлайн карты (конкурентное преимущество)
8. **Промт 5.1** - iOS нативное приложение (платформа)
9. **Промт 5.3** - Android нативное приложение (платформа)
10. **Промт 4.1** - Оптимизация производительности (качество)

---

# 📚 ИСТОЧНИКИ И РЕФЕРЕНСЫ

## Amap Технические статьи:
- https://www.cnblogs.com/amap_tech/p/11865230.html - Архитектура Amap (китайский)
- https://developer.amap.com/ - Amap Open Platform документация

## Open Source Проекты:
- **MapLibre GL** - Open source map rendering (https://github.com/maplibre/maplibre-gl-js)
- **Valhalla** - Open source routing (https://github.com/valhalla/valhalla)
- **OSRM** - Open Source Routing Machine (https://github.com/Project-OSRM/osrm)
- **OpenStreetMap** - Map data (https://www.openstreetmap.org)
- **GraphHopper** - Another routing engine (https://github.com/graphhopper/graphhopper)

## Книги и Документация:
- "OpenGL ES 3.0 Programming Guide"
- "Effective JavaScript" для VDOM
- "High Performance Browser Networking"

---

## ✅ ЧЕКЛИСТ СТАРТА

- [ ] Запустить текущий navigation_server
- [ ] Создать React Native проект (Промт 0.11)
- [ ] Интегрировать MapLibre (Промт 1.2)
- [ ] Добавить GPS локацию (Промт 1.4)
- [ ] Подключить routing API (Промт 1.9)
- [ ] Начать turn-by-turn (Промт 1.10)

**Первый результат возможен за 7 дней если работать интенсивно.**

---

*Документ создан AI Engine на основе анализа проекта mansoni и технологий Amap.*
*Дата: 2026-04-18*