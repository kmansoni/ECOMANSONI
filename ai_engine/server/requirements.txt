# ARIA AI Server — Production Requirements
# =========================================
# Phase 1-3 dependencies

# ── Core server ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# ── Deep Learning (optional, enables full Transformer + LoRA training) ───────
# torch>=2.2.0                    # uncomment for GPU training
# torchvision>=0.17.0             # optional images

# ── ML (optional sklearn backend for reward model + safety classifier) ───────
scikit-learn>=1.4.0
numpy>=1.26.0

# ── Web crawling (optional, enables WebCrawler) ──────────────────────────────
aiohttp>=3.9.0
aiohttp[speedups]                  # optional: C extensions for performance
certifi>=2024.1.1                  # SSL certificates

# ── Language detection (optional, enables DataPipeline lang detection) ───────
langdetect>=1.0.9

# ── Monitoring (optional, enables Prometheus metrics endpoint) ───────────────
prometheus-client>=0.20.0

# ── Observability (optional, enables OpenTelemetry tracing) ─────────────────
# opentelemetry-api>=1.23.0
# opentelemetry-sdk>=1.23.0
# opentelemetry-exporter-otlp>=1.23.0

# ── Database ─────────────────────────────────────────────────────────────────
# sqlite3 is included in stdlib — no extra package needed

# ── Dev / testing ────────────────────────────────────────────────────────────
# httpx>=0.26.0                   # for FastAPI TestClient
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
