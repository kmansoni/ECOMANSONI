# Отчёт Mansoni

| Параметр | Значение |
|---|---|
| Дата | 2026-04-07 10:56 |
| Задача | Security scan v3 — с улучшенной фильтрацией |
| Тип | security |
| Пайплайн | security |
| Находок | 31 |
| Время | 10.3с |

---

## Агент: security
Находок: 31

### HIGH (1)
- **xss** `src/pages/AIAssistantPage.tsx:833`: XSS риск: dangerouslySetInnerHTML без sanitize
  - Fix: Использовать DOMPurify

### MEDIUM (15)
- **performance** `services/email-router/src/db.ts:459`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/components/feed/StoryViewer.tsx:58`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/components/feed/StoryViewer.tsx:59`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/components/feed/StoryViewer.tsx:60`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/components/feed/StoryViewer.tsx:61`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/components/feed/StoryViewer.tsx:62`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/hooks/useAchievements.ts:102`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/hooks/useAchievements.ts:103`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/hooks/useAchievements.ts:104`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/hooks/useAdCampaigns.ts:256`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/hooks/useAdCampaigns.ts:257`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/hooks/useAdCampaigns.ts:258`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/hooks/useChat.tsx:418`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/hooks/useChat.tsx:419`: Supabase запрос без .limit()
  - Fix: Добавить .limit()
- **performance** `src/hooks/useChat.tsx:845`: Supabase запрос без .limit()
  - Fix: Добавить .limit()

### LOW (15)
- **rls** `supabase/migrations`: Таблица `email_events_default` без RLS (только server-side)
  - Fix: ALTER TABLE email_events_default ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `events_raw_v1` без RLS (только server-side)
  - Fix: ALTER TABLE events_raw_v1 ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `events_rollup_5m_v1` без RLS (только server-side)
  - Fix: ALTER TABLE events_rollup_5m_v1 ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `events_rollup_day_v1` без RLS (только server-side)
  - Fix: ALTER TABLE events_rollup_day_v1 ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `nav_dispatch_analytics` без RLS (только server-side)
  - Fix: ALTER TABLE nav_dispatch_analytics ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `nav_location_events` без RLS (только server-side)
  - Fix: ALTER TABLE nav_location_events ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `nav_location_hourly_agg` без RLS (только server-side)
  - Fix: ALTER TABLE nav_location_hourly_agg ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `nav_search_analytics` без RLS (только server-side)
  - Fix: ALTER TABLE nav_search_analytics ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `nav_search_daily_queries` без RLS (только server-side)
  - Fix: ALTER TABLE nav_search_daily_queries ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `nav_surge_history` без RLS (только server-side)
  - Fix: ALTER TABLE nav_surge_history ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `nav_surge_hourly_agg` без RLS (только server-side)
  - Fix: ALTER TABLE nav_surge_hourly_agg ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `nav_traffic_history` без RLS (только server-side)
  - Fix: ALTER TABLE nav_traffic_history ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `nav_trip_daily_funnel` без RLS (только server-side)
  - Fix: ALTER TABLE nav_trip_daily_funnel ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `nav_trip_events` без RLS (только server-side)
  - Fix: ALTER TABLE nav_trip_events ENABLE ROW LEVEL SECURITY
- **rls** `supabase/migrations`: Таблица `suppression_list` без RLS (только server-side)
  - Fix: ALTER TABLE suppression_list ENABLE ROW LEVEL SECURITY
