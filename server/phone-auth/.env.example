# Phone Authentication Service

These are placeholder environment variables for the phone-auth service.
Copy to `.env.local` and update with actual values.

# Server Configuration
PHONE_AUTH_PORT=3001

# Database Configuration (PostgreSQL)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://mansoni_user:CHANGE_ME@localhost:5432/mansoni

# JWT Configuration
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_TO_SECURE_KEY

# OTP Configuration
OTP_VALIDITY_SEC=300
OTP_MAX_ATTEMPTS=5

# SMS Provider
# Options: 'stub' (logs to console), 'twilio'
SMS_PROVIDER=stub

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3001

# Environment
NODE_ENV=development
