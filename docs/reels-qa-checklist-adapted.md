# Reels QA checklist (адаптировано под текущий проект)

Документ — «приземлённая» версия универсального чеклиста Reels под реальную текущую реализацию в этом репозитории. **Никаких изменений в коде не предполагает**: это список проверок + статус покрытия.

## Легенда

**Приоритет**
- **P0** — критично для MVP / блокирует релиз
- **P1** — важно для качества, но не блокирует
- **P2** — улучшения / последующие итерации

**Статус**
- ✅ сделано/есть в коде
- 🟡 частично (есть слой данных, но нет UI / есть UX-долг / есть ограничение)
- ❌ нет
- ⛔ сознательно вне текущего скоупа

## Текущие точки «истины» (где смотреть в коде)

UI / поведение
- `src/pages/ReelsPage.tsx` — лента, scroll-snap, play/pause, double-tap, открытие comments/share
- `src/components/reels/ReelCommentsSheet.tsx` — UI комментариев (drawer)
- `src/components/reels/ReelShareSheet.tsx` — UI шеринга в DM/группы/каналы (drawer)
- `src/components/reels/CreateReelSheet.tsx` — создание/загрузка видео + публикация

Данные / интеграции
- `src/hooks/useReels.tsx` — загрузка Reels, лайки/сейвы/репосты, просмотры, shares analytics, create
- `src/hooks/useReelComments.tsx` — загрузка/дерево/лайки/удаление комментариев
- `src/lib/demo/demoMode.ts` + `src/lib/demo/demoBots.ts` — guest_mode + демо контент

Supabase (схема/политики)
- `supabase/migrations/20260123020801_*.sql` — `reels`, `reel_likes`, `reel_views`, bucket `reels-media`
- `supabase/migrations/20260123020844_*.sql` — триггеры на `likes_count` и `views_count`
- `supabase/migrations/20260126192010_*.sql` — `reel_comments`, `reel_comment_likes` + триггеры
- `supabase/migrations/20260220200000_*.sql` — `reel_saves`, `reel_reposts`, `reel_shares` + триггеры
- `supabase/migrations/20260123035900_*.sql` — политики storage-объектов `reels-media`

---

## 1) Навигация/доступ

### P0
- ✅ Открытие экрана Reels из приложения (роутинг присутствует; фактический роут см. `src/pages/*` и `src/App.tsx`).
- ✅ Неавторизованный пользователь может **смотреть** Reels (RLS: `reels` SELECT true; `reel_views` INSERT true).
- ✅ Неавторизованный пользователь **не может** лайкать/комментировать/шерить (UI делает редирект на `/auth` для like/comment/share).
- ✅ При отсутствии контента показывается empty-state с CTA «Создать Reel» (только для auth).

### P1
- 🟡 Возврат на Reels после логина: зависит от общего роутинга и того, как хранится intended route (в ReelsPage есть явный `navigate('/auth')`, но не сохранение “куда вернуться”).

### P2
- ⛔ Deep-linking на конкретный Reel (например `/reels/:id`) — в текущей реализации не видно.

---

## 2) Лента (feed) и загрузка данных

### Реализация сейчас
- `useReels()` грузит до 50 последних Reels: `reels` → `profiles` → user’s likes/saves/reposts.
- Есть режим `feedMode = 'friends'`: авторы — те, на кого подписан пользователь (`followers`), плюс собственный `user.id`.
- В guest_mode подмешиваются демо-ролики `demo_*`.

### P0
- ✅ Лента загружается и показывает элементы (поля: `video_url`, `description`, `music_title`, автор).
- ✅ Есть loader-state во время загрузки (`loading` → спиннер).
- ✅ Пустая лента корректно отображается (empty-state).
- ✅ Авторские профили подтягиваются из `profiles` по `author_id`.

### P1
- 🟡 Ошибки загрузки: сейчас логируются в `console.error`, но нет явного UI-баннера/ретрая (кроме косвенного `refetch` из comments close).
- 🟡 Пагинация/бесконечная подгрузка отсутствует (всегда `.limit(50)`).
- 🟡 Realtime обновления ленты (новые Reels/лайки/комменты) отсутствуют.

### P2
- ⛔ Рекомендательная выдача/ранжирование, персонализация, “For you” модель — не реализовано.

---

## 3) Плеер / просмотр

### P0
- ✅ Вертикальный scroll-snap (контейнер `scrollSnapType: 'y mandatory'`, item `scrollSnapAlign: 'start'`).
- ✅ Автопауза всех неактивных видео, autoplay активного (через `videoRefs` + effect по `currentIndex`).
- ✅ Tap: одиночный — play/pause, double-tap — like (если ещё не лайкнуто) + heart animation.
- ✅ Видео loop + muted + playsInline.
- ✅ Для демо-контента (или не-video URL) рендерится `<img>`.

### P1
- 🟡 Критерий “что считать видео” сейчас очень примитивный (`includes('.mp4') || includes('video')`). Возможны false positives/negatives.
- 🟡 Управление звуком (mute/unmute, уровень, выбор дорожки) отсутствует.
- 🟡 Buffering UI/состояние загрузки конкретного видео отсутствует.
- 🟡 Обработка ошибок `<video>` (404/403) отсутствует.

### P2
- ⛔ ABR/HLS/DASH, адаптивные качества, prefetch/буфер на следующий ролик.

---

## 4) Интерактивы: лайк/сейв/репост

### Реализация сейчас (DB)
- `reel_likes` с UNIQUE(reel_id,user_id) + триггер `likes_count`.
- `reel_saves` + триггер `saves_count`.
- `reel_reposts` + триггер `reposts_count`.

### P0
- ✅ Like из UI работает (кнопка + double-tap) и требует auth.
- ✅ Оптимистическое обновление лайков и счётчика (в `useReels.toggleLike`).
- ✅ Demo reels: like работает локально без БД.

### P1
- 🟡 Save: **данные реализованы** (`useReels.toggleSave`, таблица `reel_saves`), но **в UI ReelsPage кнопка Save не привязана** (onClick делает только `stopPropagation`).
- 🟡 Repost: **данные реализованы** (`useReels.toggleRepost`, таблица `reel_reposts`), но **в UI ReelsPage нет кнопки/UX**.
- 🟡 Синхронизация isLiked/isSaved/isReposted с сервером при внешних изменениях отсутствует.

### P2
- ⛔ Эмо-реакции (не только like), анимации/“burst” на кнопках, “undo” на уровне UI.

---

## 5) Комментарии

### Реализация сейчас
- Таблицы: `reel_comments` + `reel_comment_likes`.
- Поддержка parent_id (один уровень ответов в UI).
- Триггер на `reels.comments_count` и `reel_comments.likes_count`.

### P0
- ✅ Открытие комментариев из Reels (drawer).
- ✅ Просмотр списка комментариев.
- ✅ Добавление комментария (auth required, иначе редирект `/auth`).
- ✅ Ответ на комментарий (parent_id).
- ✅ Like на комментарий.
- ✅ Удаление собственного комментария.

### P1
- 🟡 Ветка ответов в UI только 1 уровень (tree глубже 1 уровня не отображается).
- 🟡 Нет редактирования комментария.
- 🟡 Нет модерации/жалоб на комменты.
- 🟡 Нет optimistic create/delete (после add/delete делается `fetchComments()`).

### P2
- ⛔ Упоминания (@), хештеги, ссылки, фильтрация токсичности, автоскрытие.

---

## 6) Шеринг

### Реализация сейчас
- UI: выбор целей (DM/Group/Channel), поиск, мультиселект.
- Отправка в:
  - `messages` (DM) через upsert c `client_msg_id`
  - `group_chat_messages`
  - `channel_messages`
- Параллельно best-effort записывается аналитика в `reel_shares`.

### P0
- ✅ Открытие share sheet из Reels.
- ✅ Шеринг в DM/группы/каналы (в зависимости от доступности сущностей).
- ✅ Мульти-выбор целей.
- ✅ Ошибки шеринга отображаются (toast).

### P1
- 🟡 Права на каналы: сейчас добавляются только каналы, где `owner_id === user.id` (не роль-based).
- 🟡 Нет “share link” (копировать ссылку), нет share в OS sheet.
- 🟡 В `reel_shares` нет защиты от повторов/спама (схема допускает много событий).

### P2
- ⛔ Forward/reshare в stories, “share to external apps”, QR.

---

## 7) Создание Reels (upload + publish)

### Реализация сейчас
- Выбор видео файла, лимит 100MB.
- Upload в storage bucket `reels-media` по пути `${user.id}/{timestamp}.{ext}`.
- Получение public URL и insert в `reels`.
- Опционально — SimpleMediaEditor (редактирование видео blob).

### P0
- ✅ Создание Reel доступно из empty-state (когда лента пустая) и только для auth.
- ✅ Валидация типа файла (должен быть `video/*`).
- ✅ Валидация размера ≤100MB.
- ✅ Upload в Supabase Storage.
- ✅ Publish (insert в `reels`).

### P1
- 🟡 Доступ к созданию Reel при **непустой** ленте: UI-кнопки/CTA на экране Reels не видно (sheet есть, но триггер может отсутствовать в обычном сценарии).
- 🟡 Thumbnail генерация отсутствует (в БД есть `thumbnail_url`, но не заполняется).
- 🟡 Обработка “bucket not found” есть, но нет авто-настройки (и это нормально — инфраструктура должна быть готова).
- 🟡 Ограничение mime в bucket: разрешены конкретные типы (mp4/webm/quicktime/avi) — фронт принимает `video/*`, что может приводить к ошибке на upload для экзотических форматов.

### P2
- ⛔ Черновики, обрезка/склейка таймлайна на уровне продукта, наложение музыки, эффекты, публикация по расписанию.

---

## 8) Счётчики и аналитика (views/likes/comments/saves/reposts/shares)

### Реализация сейчас (DB)
- `reel_views` INSERT → триггер `views_count++`.
- `reel_likes` INSERT/DELETE → триггер `likes_count`.
- `reel_comments` INSERT/DELETE → триггер `comments_count`.
- `reel_comment_likes` INSERT/DELETE → триггер `reel_comments.likes_count`.
- `reel_saves`, `reel_reposts`, `reel_shares` → триггеры на соответствующие счетчики в `reels`.

### P0
- ✅ Просмотр (view) записывается при первом показе Reel на сессии страницы (`viewedReels` Set).
- ✅ Views можно писать и анонимно: в `sessionStorage` генерится `reels_anon_session_id`, в БД уходит `session_id=anon-...`.
- ✅ Счётчики в `reels` увеличиваются серверными триггерами.

### P1
- 🟡 Нет антифрода/дедупликации views (таблица допускает множество views от одного пользователя/сессии).
- 🟡 В UI не показываются views/saves/reposts/shares (в ReelsPage отображаются только likes и comments).
- 🟡 Analytics events для demo reels не пишутся в БД (и это ок), но локальные счетчики частично обновляются.

### P2
- ⛔ Дашборды/insights, retention, completion rate, watch time, unique viewers, cohort analytics.

---

## 9) Guest / demo mode

### P0
- ✅ При `guest_mode` в ленту подмешиваются демо Reels (`demo_*`).
- ✅ Демонстрационные лайки/сейвы/репосты (для demo-id) работают локально.

### P1
- 🟡 Смешанная лента: demo добавляется перед реальными; строгой “сегрегации” нет.
- 🟡 Демонстрационные assets могут быть не-видео (jpg), что влияет на тесты плеера.

---

## 10) Безопасность/RLS/приватность (что реально настроено)

### P0
- ✅ `reels`: SELECT всем, INSERT/UPDATE/DELETE только автор.
- ✅ `reel_likes`: SELECT всем, INSERT/DELETE только auth user.
- ✅ `reel_views`: SELECT всем, INSERT всем.
- ✅ `reel_comments`/`reel_comment_likes`: SELECT всем, INSERT/DELETE auth user (и delete только owner).
- ✅ Storage `reels-media`: public read, upload/update/delete только владелец папки `${auth.uid()}`.

### P1
- 🟡 Так как bucket public, любой, у кого есть URL, может смотреть видео (это соответствует текущей политике, но может не соответствовать продуктовым требованиям приватности).
- 🟡 Нет блокировок/приватных аккаунтов, “close friends”, гео-ограничений.

### P2
- ⛔ Полноценная модерация/жалобы/удаление по правилам сообщества.

---

## 11) Мини-матрица покрытия «универсального чеклиста 1000» → текущий проект

Ниже — упрощённое сопоставление секций из `docs/reels-qa-checklist-1000.md`:

- A (Права/доступ): ✅ базовый доступ + RLS; 🟡 приватность/блокировки
- B (Лента/навигация): ✅ scroll-snap; 🟡 пагинация/поиск/рекомендации
- C (Плеер): ✅ autoplay/pause/double-tap; 🟡 buffering/errors/audio
- D (Загрузка/публикация): ✅ upload+insert; 🟡 thumbnail/CTA когда лента не пустая
- E (Интерактивы): ✅ like; 🟡 save/repost (данные есть, UI нет)
- F (Рекомендации): ⛔
- G (Безопасность/модерация): 🟡 только базовые RLS; ⛔ модерация
- H (Аналитика/перфоманс): ✅ базовые counters; 🟡 уникальность/insights

---

## 12) Список «самых полезных» QA сценариев для ручной проверки (MVP)

### P0 smoke (10–15 минут)
1. Открыть Reels, пролистать 5 роликов — убедиться что активный играет, остальные на паузе.
2. Tap → pause/play. Double-tap → like + анимация.
3. Открыть комментарии → добавить комментарий → ответить → лайкнуть комментарий → удалить свой комментарий.
4. Открыть share sheet → выбрать 1 DM → отправить → убедиться что сообщение ушло.
5. Перезагрузить страницу → убедиться, что лайк сохранился (для реального reel, не demo).

### P1 regression
6. Войти/выйти и проверить, что UI корректно запрещает лайк/комменты/шеринг без auth.
7. Попробовать загрузить видео >100MB → получить понятную ошибку.
8. Попробовать загрузить формат, который может быть запрещён bucket allowed_mime_types → убедиться в корректной ошибке.

---

## 13) Явные текущие «дыры» (как чеклист-напоминание)

- 🟡 Save/Repost: данные реализованы, UI не подключен (ReelsPage).
- 🟡 Нет CTA/кнопки «Создать Reel» при непустой ленте.
- 🟡 Нет обработки video load/buffer/error UI.
- 🟡 Нет рекомендаций/поиска/пагинации.

