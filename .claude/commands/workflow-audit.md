# /workflow-audit

Установи runtime workflow context для аудита.

Выполни:

```powershell
node .claude/helpers/workflow-context.cjs workflow audit
node .claude/helpers/workflow-context.cjs review-stage review-start
```
