# /workflow-feature

Установи runtime workflow context для фичи.

Выполни:

```powershell
node .claude/helpers/workflow-context.cjs workflow feature
```

После этого работай по каноническому feature pipeline Mansoni.
