-- Minimal project seed
-- Keep deterministic and idempotent; avoid secrets and environment-specific values.

begin;

-- Intentionally empty baseline seed. Domain seeds should be added in separate, versioned files.

commit;
