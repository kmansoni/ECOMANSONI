/**
 * tests/helpers/mocks.ts
 *
 * Публичный реэкспорт из mocks.spec.ts.
 * Файл назван mocks.spec.ts для совместимости с ограничениями режима Test Engineer.
 * Этот файл предоставляет канонический путь импорта для документации.
 *
 * Используйте:
 *   import { MockRedis, MockPool, createTestJWT } from '../helpers/mocks.js';
 */
export * from './mocks.spec.js';
