/**
 * Call State Machine — re-exports from calls-v2 for clean domain access.
 * Single source of truth for call lifecycle FSM.
 */

// Re-export everything from calls-v2
export * from "@/calls-v2/callStateMachine";
