/**
 * Brand gradient background with animated orbs in logo colors (blue → teal → green)
 */
export function BrandBackground() {
  // Backgrounds are centrally defined in src/index.css and must not be overridden per-page.
  return null;
}
